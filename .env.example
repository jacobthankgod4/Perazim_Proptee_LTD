# ============================================================================
# DATABASE CONFIGURATION — Supabase Postgres (REQUIRED)
# ============================================================================
# Get DATABASE_URL from Supabase project settings:
# Dashboard → Project Settings → Database → Connection Pooling (Recommended)
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL=postgresql://postgres:CHANGE_ME@db.xxxxx.supabase.co:6543/postgres

# ============================================================================
# SUPABASE AUTH & STORAGE (REQUIRED for Phase 3-5)
# ============================================================================
# Get from Supabase Dashboard → Settings → API
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.CHANGE_ME...
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.CHANGE_ME...

# ============================================================================
# PAYMENT PROCESSOR — Paystack (REQUIRED for invest flows)
# ============================================================================
# Get from Paystack Dashboard → Settings → API Keys
PAYSTACK_PUBLIC=pk_live_CHANGE_ME...
PAYSTACK_SECRET=sk_live_CHANGE_ME...

# ============================================================================
# EMAIL SERVICE — SMTP Configuration (REQUIRED for auth notifications)
# ============================================================================
# For development, use Mailtrap or SendGrid; for production, use SMTP server
SMTP_HOST=rbx109.truehost.cloud
SMTP_PORT=587
SMTP_USER=validation@perazimpropteeltd.com
SMTP_PASS=CHANGE_ME
SMTP_FROM=validation@perazimpropteeltd.com
SMTP_FROM_NAME=PeraVest

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================
# Set to false for staging, true for production
LIVE=false

# ============================================================================
# NOTES FOR SETUP
# ============================================================================
# 1. NEVER commit this file with real credentials to git
# 2. In Vercel, set each variable via Dashboard → Settings → Environment Variables
# 3. Use different keys for staging vs. production (separate Supabase projects)
# 4. Rotate credentials every 90 days in production
# 5. Use SERVICE_KEY (not ANON_KEY) for server-side operations
# 6. For local development, create .env.local with your values (git-ignored)
